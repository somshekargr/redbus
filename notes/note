z-index property specifies the stack order of an element. An element with greater stack order is always in front of an element
with a lower stack point. 
z-index only works on positioned elements (position:absolute, fixed, stiky, relative)